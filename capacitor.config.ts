import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.jonnyramen.canvasdrawer',
  appName: 'Canvas Drawer',
  webDir: 'www'
};

export default config;
